pre-commit:
 commands:
  biome-check:
   run: pnpm format && pnpm lint
   stage_fixed: true

commit-msg:
 commands:
  lint-commit-msg:
   run: pnpm commitlint --edit

pre-push:
 commands:
  check:
   run: pnpm format && pnpm lint